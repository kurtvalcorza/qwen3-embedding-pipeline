---
license: apache-2.0
model_card_spec: "1.1"
pipeline_tag: feature-extraction
task: "Others - Text Embedding"
base_model: Qwen/Qwen3-Embedding-0.6B
date_published: "2025-06-03"
date_published_source: "Hugging Face Hub repository creation date of the exact hosted checkpoint (`createdAt`, https://huggingface.co/api/models/Qwen/Qwen3-Embedding-0.6B)"
---

# Qwen3-Embedding-0.6B — Text Embedding Model (Encoder)

[![Hugging Face](https://img.shields.io/badge/%F0%9F%A4%97%20Hugging%20Face-Qwen%2FQwen3--Embedding--0.6B-ffcc4d?style=flat)](https://huggingface.co/Qwen/Qwen3-Embedding-0.6B)
[![Upstream GitHub](https://img.shields.io/badge/Upstream%20GitHub-QwenLM%2FQwen3--Embedding-181717?style=flat&logo=github&logoColor=white)](https://github.com/QwenLM/Qwen3-Embedding)
[![arXiv Paper](https://img.shields.io/badge/arXiv-2506.05176-b31b1b.svg)](https://arxiv.org/abs/2506.05176)
[![License: Apache-2.0](https://img.shields.io/badge/License-Apache--2.0-blue.svg)](https://opensource.org/licenses/Apache-2.0)

> [!WARNING]
> ⚠️ **Provided for research, training, and evaluation purposes only.** Model weights are redistributed unmodified under their upstream license, which controls your use, including any commercial use or redistribution; the accompanying code and notebooks are released under this repository's license. All of it is supplied **"as is"**, without warranty of any kind, and has not been validated for production, clinical, or safety-critical use. Running the notebooks downloads third-party weights and datasets governed by their own licenses and consumes compute on your own Colab/Kaggle account. To the maximum extent permitted by law, the maintainers of this repository and the DIMER platform accept no liability for any damages arising from their use. Hosting implies no affiliation with or endorsement by the original authors.

---

## Interactive Colab Tutorials

This pipeline provides a ready-to-run interactive Google Colab notebook that exercises the repository's public API end to end — bootstrap a fresh runtime, stage and verify the pinned upstream revision, fetch and validate a digest-pinned labelled corpus, embed queries and documents through the inference contract, read the frozen embedder's retrieval metrics on held-out queries beside a random floor and a lexical baseline, adapt the last decoder layers with a bounded contrastive fine-tuning, evaluate on an independent test split, compare retrieved documents before and after, and export a reloadable adapter:

- **End-to-End Fine-Tuning Tutorial**:  
  [![Open In Colab](https://colab.research.google.com/assets/colab-badge.svg)](https://colab.research.google.com/github/kurtvalcorza/qwen3-embedding-pipeline/blob/main/tutorials/qwen3_embedding_colab.ipynb) [`qwen3_embedding_colab.ipynb`](https://github.com/kurtvalcorza/qwen3-embedding-pipeline/blob/main/tutorials/qwen3_embedding_colab.ipynb)  
  *Text embeddings and bounded contrastive fine-tuning with the pinned `Qwen/Qwen3-Embedding-0.6B` weights: 1024-d last-token-pooled, L2-normalised vectors with instruction-aware queries; 616 / 154 / 385 Banking77 message → intent-phrase pairs (CC BY 4.0, two pinned CSV files) validated and split without leakage over all 77 intents; the frozen embedder's held-out **recall@1** (63.4 %) and MRR (0.742) over the 77 intent documents read beside the random floor (1.3 %) and a lexical baseline (30.6 %); the last two decoder layers (31,461,888 of 595,776,512 parameters) trained for two epochs with InfoNCE and validation-MRR epoch selection; the adapted embedder scored on the held-out split (recall@1 80.3 %, MRR 0.879), its top-3 retrievals for three test queries printed beside the frozen ones, and exported as a 126 MB safetensors adapter whose reload reproduces the vectors and the metric.*

---

#### Description

`Qwen/Qwen3-Embedding-0.6B` is the smallest of the Qwen3 Embedding series (Zhang et al., arXiv:2506.05176), pinned here to revision `97b0c614be4d77ee51c0cef4e5f07c00f9eb65b3`. The upstream README states it is built on `Qwen/Qwen3-0.6B-Base`; the pinned `config.json` is a `Qwen3ForCausalLM` decoder with 28 layers, hidden size 1024, 16 attention heads over 8 key-value heads, and a 32,768-token position budget. Used as an embedder, the decoder runs one forward pass over the text and the hidden state at the last token position is taken as the sentence vector (`1_Pooling/config.json`: `pooling_mode_lasttoken: true`), then L2-normalised; queries are prefixed with a task instruction, documents are not. Nothing is conditioned at inference. What this repository adds is the `Qwen3EmbeddingPipeline` class in `src/qwen3_embedding_pipeline/pipeline.py`: manifest verification (`verify_snapshot`), fresh-clone staging (`stage_missing_files`), a loader that refuses remote code and mutable caches, input validation with named ceilings, the README's exact prefix/pooling/normalisation contract, provenance fields in every result; and, for adaptation, `evaluate` (retrieval recall@k and MRR of a validated query–positive dataset over its document set), `lexical_baseline` (the no-model floor), `adapt` (InfoNCE contrastive fine-tuning of the last decoder layers with validation-MRR epoch selection) and `save_artifact` / `from_artifact` (a safetensors adapter bound to the base weights), with the dataset contract in `samples.py` (the digest-pinned Banking77 pairs, BYOD loaders) and the metric implementations in `metrics.py`.

#### Intended Use and Limitations

###### Primary Intended Uses

The task is text embedding: input is a list of up to `MAX_BATCH = 64` strings with a `kind` of `"query"` or `"document"`; output is one 1024-dimensional unit-norm float32 vector per string, so that the dot product of a query vector and a document vector is their cosine similarity. Envisioned applications are dense retrieval over an operator's own corpus (documents embedded once, queries at request time), semantic deduplication and clustering of text collections, and use as a fixed feature extractor feeding a downstream classifier the operator trains; the upstream README also names code retrieval and bitext mining across the 100+ languages it claims. In a larger system the pipeline is the encoding component of a retrieval stack, paired with a vector index and, where precision matters, the sibling `qwen3-reranker-pipeline` on the top candidates.

###### Primary Intended Users

Intended users are machine-learning engineers, search and data engineers, and application developers building retrieval or clustering features for research prototypes or in-house search. The pipeline assumes its users know that an embedding is a representation with no label attached, that cosine similarity between two vectors is only meaningful relative to other pairs from the same model, that queries and documents must be embedded with the matching `kind` (mixing them silently degrades retrieval), that text beyond 8,192 tokens is cut off, and that retrieval quality on their own corpus must be measured with their own relevance judgements before deployment.

###### Out-of-scope use cases

1. **Capability boundary:** the model does not generate text, answer questions, classify into a fixed label set, or score a query-document pair directly; pair scoring is the sibling `qwen3-reranker-pipeline`, and text generation belongs to the language-model pipelines. It emits no token-level or span-level output. The fine-tuning that is exposed trains only the last decoder layers on query–positive pairs with in-batch (and explicit) negatives — no classification head, no hard-negative mining, no full-model or embedding-table training.
2. **Input boundary:** `embed()` takes a list of 1 to 64 non-empty strings (`MAX_BATCH`; a bare string raises `TypeError`, an empty list or empty item raises `ValueError`); any item above 100,000 characters (`MAX_TEXT_CHARS`) is rejected before tokenisation, and any item that tokenises beyond 8,192 tokens (`MAX_TEXT_TOKENS`) is truncated and flagged `truncated: True`. Only `kind` values `"query"` and `"document"` are accepted. Images, audio, and structured records are not inputs.
3. **Adaptation boundary:** `evaluate` and `adapt` refuse a dataset outside 8..20,000 records or the `{id, query, positive}` contract, a document set outside 2..1,000 candidates, or a positive missing from the candidates; `adapt` truncates queries and documents to `MAX_TRAIN_TOKENS = 64` during training only, trains at most the 28 decoder layers, and keeps the epoch with the highest validation MRR; a retrieval gain on one document set is not evidence about any other, and the adapter changes layers every input shares, so every vector shifts and cosine values are not comparable across the frozen and adapted models.
3. **Decision boundary:** not for autonomous decisions about people from similarity scores — candidate screening, content moderation, fraud flags, or eligibility — without a human reviewing the retrieved items; a similarity score carries no calibrated meaning and the model's training data is not disclosed.

#### Factors

###### Groups

The pipeline is not human-centric by design — it maps arbitrary text to vectors — but the text it embeds routinely describes or is written by people, and the upstream pretraining and contrastive fine-tuning corpora are not enumerated or group-audited by the Qwen team in the pinned README. No group-level audit exists here either. Embedding bias transfers directly into the operator's retrieval pipeline: if names, dialects, or topics associated with a demographic group sit systematically closer to or farther from certain queries, ranking is skewed for that group. The downstream operator must run a fairness audit on their own retrieval task — for example, retrieval rate and rank position for otherwise-equivalent queries and documents that differ only in a name, gender marker, or language variety — before relying on the ranking.

###### Instrumentation

There is no physical sensor: the training data is text, produced by keyboards, web crawls, transcription, and machine translation, then tokenised by the Qwen2 byte-pair tokenizer (`tokenizer.json`, 151,669-entry vocabulary). The characteristics that materially affect the data are encoding, normalisation, and language mix at collection time, none of which the upstream README describes beyond the "100+ languages" claim and the note that most training instructions were written in English. At inference the pipeline consumes strings exactly as the caller passes them: no case folding, whitespace normalisation, HTML stripping, or language detection. Defects in the operator's upstream text pipeline — mis-decoded bytes, boilerplate, concatenated records, wrong-language text — reach the model unchanged, and the pipeline can detect only the shape errors it validates (type, emptiness, length ceilings) and the truncation it reports.

###### Environment

Operating environment: Python 3.12 with `torch==2.14.0` (the venv build is `2.14.0+cu130`), `transformers==4.57.6`, `huggingface-hub==0.36.2`, `safetensors==0.8.0`, `numpy==2.5.3` (`pyproject.toml`). The loader runs float32 on CPU and bfloat16 on CUDA (the checkpoint's native `torch_dtype`); the recorded smoke ran on CPU only (`CUDA_VISIBLE_DEVICES=-1`, `device="cpu"`), loading 1.19 GB of weights in 6.63 s and embedding four short texts in 0.44 s on the host described under "Runtime". Flash attention and the CUDA path were not exercised. Data environment: the model assumes natural-language or code text resembling its undisclosed web-scale training mix, with queries carrying an English task instruction as the README recommends; retrieval quality degrades, without any error, on domains far from that mix (specialised jargon, tables serialised as text, very short or very long inputs near the 8,192-token cut) and on languages with thin coverage.

#### Metrics

###### Performance Measures

Four retrieval performance measures over a labelled query–positive dataset, implemented in `src/qwen3_embedding_pipeline/metrics.py` with no external scorer: **recall@1**, **recall@5** and **recall@10** (the query's positive document is ranked within the first *k* of the document set by cosine) and **MRR** (mean of 1 / rank), with `median_rank` beside them; ties are counted against the positive. `retrieval_metrics(ranks, n_documents)` aggregates 1-based ranks; `pipe.evaluate(records, instruction=..., candidates=None)` embeds the document set (the dataset's sorted unique positives, or the supplied candidates) once and every query with the instruction through the public `embed` contract, ranks by cosine and returns the metrics with `n_queries`, `n_documents`, the verdict `measured` (≥ `MIN_SCORED_RECORDS = 50` records) or `measured-small-sample`, `adapted`, the elapsed time and the model identity; `random_floor(n_documents)` gives the expected values of a uniformly random ranking and `pipe.lexical_baseline(records)` ranks by Jaccard token overlap — the no-model floor. Validation MRR selects the epoch in `adapt`. These are ranking metrics because retrieval is the intended use; they say whether the gold document ranks first for the queries of one labelled set, not whether the vectors are good for clustering, classification or another corpus. `embed()` itself still returns representations without a score and `cosine_similarity()` is a comparison helper; the public per-batch `evaluation_report()` helper still returns `not-measurable` with an empty `metrics` list, because a batch of vectors has no ground truth without a labelled set. Upstream reports MTEB scores for this checkpoint (for example a multilingual mean-task score of 64.33 in the pinned README's evaluation table); those are upstream-reported, were not reproduced by this pipeline, and are not comparable to the intent-retrieval figures below. Recorded values on the seeded Banking77 test split (385 queries against the 77 intent documents, instruction `Given a customer support message, retrieve the banking intent it expresses`, CPU float32): random floor recall@1 1.3 %, MRR 0.064; lexical baseline recall@1 30.6 %, MRR 0.416; frozen model recall@1 63.4 %, recall@5 88.6 %, recall@10 94.0 %, MRR 0.742; adapted model (last two decoder layers, best of two epochs) recall@1 80.3 %, recall@5 97.9 %, recall@10 99.5 %, MRR 0.879 — a recall@1 change of +16.9 points.

###### Decision thresholds

No decision threshold is shipped, implicit or explicit: `embed()` applies no argmax, no cutoff, and no nearest-neighbour rule, and `cosine_similarity()` returns raw values in [-1, 1]. A threshold was withheld because the meaningful cutoff depends on the corpus, the query distribution, and the cost of a missed versus a spurious retrieval, none of which this repository can see. The operator owns it: choose a similarity cutoff or a top-k from precision-recall curves on labelled query-document pairs from the deployment, lowering it where a missed relevant document costs more than reviewing extra candidates and raising it where noise is expensive. The smoke run's values (0.7646 for a matching pair, 0.1414 for a mismatched one) are two observations reproduced from the upstream README, not calibration points. The one selection rule in the adaptation path is explicit: `adapt` keeps the epoch with the highest validation MRR (epoch 0 is the frozen model, so a run that never improves returns the base weights unchanged) and reports `best_epoch` and the per-epoch history; whether the resulting gain on the test split is worth deploying — and whether the shift in every vector is acceptable for the operator's other uses of the embedder — is the operator's decision and no threshold is shipped for it.

###### Approaches to uncertainty and variability

Every reported retrieval metric is one value on one seeded split of one corpus: `build_sample_dataset(seed=42)` fixes the balanced 616 / 154 / 385 draw from Banking77's own train/test members, `adapt(seed=0)` fixes the shuffle order and batch composition, and no repetition over seeds or splits is performed, so no confidence interval or standard deviation is available and none is claimed — the +16.9-point recall@1 gain is a point estimate whose spread is unknown; `median_rank` is the only dispersion shown. Training is deterministic on one CPU for one seed and library set but not bit-reproducible across devices or `torch` builds, and on CUDA it runs in bfloat16. Within the pipeline the forward pass is deterministic on a fixed device and dtype — there is no sampling, dropout is inactive under `torch.inference_mode`, and no seed is needed — but bfloat16 on CUDA versus float32 on CPU, kernel selection, and batch composition (left padding changes nothing in the pooled token but can shift low-order bits) move vector components in the third or fourth decimal place; the CPU smoke reproduced the README's four cosine values to four decimals. A cosine similarity is not a probability and is not calibrated; a caller who needs a confidence must fit a mapping from similarity to relevance on their own labelled pairs.

#### Ethical considerations and biases

###### Data

The upstream README discloses only that the model is built on `Qwen3-0.6B-Base`, supports 100+ languages, and that its training instructions were mostly English; the technical report (arXiv:2506.05176) describes large-scale synthetic and public retrieval pairs for contrastive training, and the base model's pretraining corpus is web-scale text that Qwen does not enumerate. Whether personal, copyrighted, or otherwise restricted text is included is therefore unknown, not ruled out. This repository distributes code, tests, and documentation; it does not vendor the weights in Git (`weights/qwen3-embedding-0.6b/model.safetensors` is git-ignored and reproduced from the pinned revision via `stage_missing_files`). The tutorial's adaptation corpus is Banking77 (Casanueva et al., 2020; CC BY 4.0) — 13,083 customer-support messages with 77 intent labels, fetched at run time as two digest-pinned CSV files from the `PolyAI-LDN/task-specific-datasets` repository at commit `57ec275d8078af65b7731c2a98be812d844a6d6b` into the git-ignored `weights/banking77/` cache and refused on any byte or SHA-256 mismatch; the repository redistributes none of it. The messages are the upstream authors' published research data; the pipeline does not inspect them for personal content. The operator must audit the text they embed for personal, confidential, or legally restricted content and must treat the resulting vectors as derived personal data where the source text was; the pipeline performs no such check.

###### Human Life

This pipeline is not intended for decisions in health, safety, criminal justice, employment, credit, or housing, and it has not been validated or certified for any of them by anyone; the only validation performed is the offline unit suite, two model-backed tests and the CPU notebook run recorded under "Runtime". Use in such domains is foreseeable — matching résumés to job descriptions, retrieving similar case files, finding related patient notes — and would be admissible only as a retrieval aid whose results a qualified person reviews, after an independent evaluation of retrieval quality and group-level behaviour on that deployment's own data, and with whatever regulatory clearance the domain requires.

###### Mitigations

1. **Supply-chain integrity:** `MODEL_ID` and the 40-hex `MODEL_REVISION` are module constants; `verify_snapshot()` checks `modelId`, `revision`, and the byte size and SHA-256 of all 11 manifest entries before any load, raising on the first mismatch (a test flips one hex digit and asserts it raises). `stage_missing_files()` fetches only manifest-listed files absent on disk, only at `MODEL_REVISION`, only when `allow_download=True`, and refuses a manifest naming another model. The loader passes `local_files_only=True` for the verified directory and `trust_remote_code=False` always.
2. **Input integrity:** `embed()` rejects a bare string, an empty or oversize batch, non-string or blank items, items above `MAX_TEXT_CHARS`, an unknown `kind`, and an empty instruction before the model runs (one test per check), and checks the backend's output shape after. The public `validate_inputs()` helper applies those same checks through the same private `_check_inputs()` function and returns an input manifest (schema, ceilings, per-input observations, verdict, findings), so a caller can record exactly what was accepted or rejected without duplicating the validation logic.
3. **Adaptation integrity:** `validate_dataset` enforces the `{id, query, positive[, negative]}` contract, unique ids, non-empty texts, the character ceilings, at least two distinct positives and the 8..20,000 record bounds before any model import; `evaluate` refuses a positive missing from the candidates and a document set outside 2..1,000; `adapt` bounds `epochs` (1..20), `lr` ((0, 1e-3]), `batch_size` (2..64), `temperature` ((0, 1]) and `trainable_layers` (1..28), validates the instruction through `_check_inputs`, trains only `layers.{k}.*` tensors and restores the best-validation state; `load_artifact` verifies the artifact format, the base model id / revision / weight digest and the file size and SHA-256 **before** deserialising, refuses any tensor that is not a decoder-layer tensor of the base or whose shape differs, and applies the tensors with `strict=True`. Offline tests cover every refusal with an injected bag-of-words runner; the model-backed tests check the frozen retrieval beats the lexical baseline and that a one-epoch adaptation reloads to identical vectors. No statistical mitigation (balancing, debiasing) is applied: the base weights are redistributed unmodified and the only training is the caller's own bounded fine-tuning on pairs the pipeline does not inspect semantically (the default sample is balanced over the 77 intents by construction).
4. **Reproducibility:** every runtime dependency is pinned with `==`; each result carries `model_id`, `model_revision`, `pooling`, `normalized`, `kind`, `instruction`, `n_tokens`, and `truncated`.
5. **Refusals:** no similarity threshold, nearest-neighbour rule, or index is exposed; the pipeline returns vectors and leaves ranking to the caller. Matryoshka truncation to fewer than 1024 dimensions, which upstream supports, is deliberately not exposed so every vector this package emits has one fixed `dim`.

###### Risks and harms

0. **Adaptation to a harmful pair set:** the contrastive contract pulls whatever pairs it is given together, including pairs that encode a biased or deceptive notion of relevance, with no content check, and every vector shifts with it; the operator who supplies the pairs owns that outcome. `adapt` on 616 short pairs takes about 133.3 s per two epochs on the reference CPU and holds gradients and AdamW moments for the trained layers.
1. **Silent truncation:** text past 8,192 tokens is dropped, so a long document is represented by its head only; the `truncated` flag is set, and an operator who ignores it retrieves on partial content. Likely for long reports and transcripts.
2. **Kind mismatch:** embedding queries as documents (or vice versa) removes the instruction prefix the model was trained with and degrades ranking without any error; the operator bears the harm as silently worse search.
3. **Bias amplification in retrieval:** systematic proximity of names, dialects, or topics to certain queries skews who or what is retrieved; the data subject bears the harm, and it compounds when the ranking feeds a human decision.
4. **Out-of-distribution confidence:** unfamiliar domains or languages still yield high-looking similarities; the operator misreads noise as relevance.
5. **Privacy and leakage:** vectors are derived from the embedded text and can leak its content through nearest-neighbour or inversion attacks on the index; an operator who treats an index as non-personal data exposes the data subjects. Magnitude ranges from degraded search to a wrongful screening outcome when the decision boundary above is ignored.

###### Use cases

The pipeline must not be used to profile individuals from the text they write, to infer protected characteristics from embeddings, for surveillance or social scoring, or for unlawful discrimination in employment, housing, credit, insurance, education, or healthcare access — including ranking people by similarity to a prototype in any of those settings. It must not be used to de-anonymise authors, to build indexes over text obtained without authorisation, or in any way that breaches the Apache-2.0 terms of the upstream weights or the terms of the deployment that runs the pipeline. These prohibitions hold even where the model would produce a plausible ranking.

## Immutable provenance

- Model: `Qwen/Qwen3-Embedding-0.6B`
- Revision: `97b0c614be4d77ee51c0cef4e5f07c00f9eb65b3`
- Manifest: `weights/qwen3-embedding-0.6b/dimer-base-manifest.json`, format `dimer_hf_snapshot` v1, 11 files, `totalBytes` 1207487471
- `model.safetensors` (1,191,586,416 bytes) SHA-256: `0437e45c94563b09e13cb7a64478fc406947a93cb34a7e05870fc8dcd48e23fd`
- `config.json` (727 bytes) SHA-256: `b5bf1f51fc45be473a54718cef92448d90a1be001bf9b9a44b8c7f10a19feaa9`
- `1_Pooling/config.json` (313 bytes) SHA-256: `37bf193fa101f19101bfad9c31d3eb0f786e247b7b1e5cb7f007d730eed1ddbd`
- Upstream reference: https://huggingface.co/Qwen/Qwen3-Embedding-0.6B

## Input/output contract

- `Qwen3EmbeddingPipeline.from_pretrained(device=None, weights_dir=None, allow_download=False)`: stages missing manifest files (only with `allow_download=True`), verifies the snapshot, loads `AutoTokenizer` (`padding_side="left"`) and `AutoModel` with `local_files_only=True`; `device` defaults to `cuda:0` when visible, else `cpu`.
- `embed(texts: list[str], kind: str = "document", instruction: str = DEFAULT_QUERY_INSTRUCTION) -> dict` with keys `embeddings` (list of 1024 float lists, unit norm), `dim` (1024), `pooling` (`"last_token"`), `normalized` (`True`), `kind`, `instruction` (the prefix used for queries, `None` for documents), `n_tokens` (per text), `truncated` (per text), `model_id`, `model_revision`.
- `cosine_similarity(a, b) -> list[list[float]]`: pairwise cosine matrix; a helper, not a metric.
- Constants: `EMBEDDING_DIM = 1024`, `MAX_TEXT_TOKENS = 8192`, `MAX_TEXT_CHARS = 100000`, `MAX_BATCH = 64`, `DEFAULT_QUERY_INSTRUCTION = "Given a web search query, retrieve relevant passages that answer the query"`.
- `evaluate(records, *, instruction=DEFAULT_QUERY_INSTRUCTION, candidates=None)` — validated `{id, query, positive[, negative]}` records (1–2,000) ranked against `candidates` (default: the dataset's sorted unique positives and negatives; 2–1,000) → `{n_queries, n_documents, recall@1, recall@5, recall@10, mrr, median_rank, definitions, instruction, verdict, adapted, seconds, model_id, model_revision}`; `lexical_baseline(records, candidates=None)` (static) → the same shape with `baseline`; `random_floor(n_documents)`; `evaluation_report(result, labels=None, *, sample_kind="synthetic")` — per-batch, always `not-measurable`.
- `adapt(train, val=None, *, instruction=DEFAULT_QUERY_INSTRUCTION, epochs=2, lr=5e-5, batch_size=16, trainable_layers=2, temperature=0.05, seed=0, progress=None)` — requires `from_pretrained`; `train` 8–20,000 records, `val` optional (1–2,000; without it the final epoch is kept); InfoNCE over each batch's unique documents (the other queries' positives are the negatives) on the last *k* decoder layers (`k` 1–28; `epochs` 1–20, `batch_size` 2–64, `lr` (0, 1e-3], `temperature` (0, 1]) with AdamW (weight decay 0.01, clip 1.0), texts truncated to `MAX_TRAIN_TOKENS = 64` during training only, and keeps the epoch with the highest validation MRR; returns `{objective, instruction, trainable_layers, n_trainable, n_total, epochs, best_epoch, selection, lr, batch_size, temperature, max_train_tokens, n_train, n_train_documents, n_val, seed, history, trainable_names, seconds}`; sets `pipe.adapter`.
- `save_artifact(directory, metadata=None)` → directory with `adapter.safetensors` (trained tensors only) + `manifest.json` (`format` = `org.valcorza.qwen3-embedding-0.6b.adapter.v1`, base `id`/`revision`/`weight_sha256`, the `instruction`, `tensors`, `sha256`, the training configuration and history); `load_artifact(directory)` verifies the manifest — the supported `format_version`, the pinned base id / revision / weight file / digest, exactly one contained `adapter.safetensors` entry whose size and SHA-256 match, and the exact decoder-layer tensor set the recorded `trainable_layers` implies — **before** deserialising, applies the tensors in place and returns the manifest (the digest detects corruption or drift of the weights relative to the adjacent manifest; it is not authenticity against an actor who can replace both files); `adapt` is transactional — if training, validation or the progress callback raises, the pre-adaptation tensors and frozen flags are restored; `Qwen3EmbeddingPipeline.from_artifact(directory, *, device=None, weights_dir=None, allow_download=False)` loads the verified base and applies it.
- `metrics.py`: `rank_of_positive(scores, positive_index)`, `retrieval_metrics(ranks, n_documents)`, `random_floor(n_documents)`, `jaccard(a, b)`, `lexical_baseline(records, documents)`, `METRIC_DEFINITIONS`. `samples.py`: `fetch_corpus(cache_dir=None, fetcher=None)`, `read_corpus(files)`, `filter_records`, `intent_phrase(intent)`, `build_sample_dataset(corpus, seed=42, sizes=SAMPLE_SPLIT)`, `fetch_sample_dataset()`, `validate_dataset(records)`, `documents(records)`, `dataset_digest`, `check_split_disjoint(splits)`, `split_dataset(records, val_fraction=0.15, test_fraction=0.2, seed=0)`, `load_byod_dataset(path)` (CSV / JSON / JSONL), `write_dataset_csv(records, path)`; constant `DEFAULT_INSTRUCTION` (the Banking77 task instruction).

## Runtime

- Pins (`pyproject.toml`): `torch==2.14.0`, `torchvision==0.29.0`, `torchaudio==2.11.0`, `transformers==4.57.6`, `huggingface-hub==0.36.2`, `safetensors==0.8.0`, `numpy==2.5.3`; dev `pytest==8.4.2`, `ruff==0.16.6`. Python 3.12, Windows venv.
- Executed 2026-09-12: `CUDA_VISIBLE_DEVICES=-1 python -m pytest -q -o addopts= tests` — 19 passed, exit 0; `ruff check src tests` clean.
- Smoke, executed on CPU (`CUDA_VISIBLE_DEVICES=-1`, `from_pretrained(device="cpu")`, float32): two README queries embedded with `kind="query"` and the two README documents with `kind="document"`; load 6.63 s, embedding 0.437 s, 7.06 s total; `dim` 1024, `n_tokens` [27, 23, 8, 31], no truncation, vector norm 1.0; cosine matrix [[0.7646, 0.1414], [0.1355, 0.6000]], equal to the upstream README's printed values to four decimals.
- Measured, adaptation (same venv and CPU, from the recorded notebook run): corpus read + split < 1 s from cache; lexical baseline < 1 s; frozen test evaluation (385 queries + 77 documents) about 27 s; `adapt` 2 epochs × 616 pairs, last two decoder layers (31,461,888 / 595,776,512 params) 133.3 s incl. per-epoch validation, validation MRR 0.699 → 0.812 → 0.828 (`best_epoch` 2); adapted test evaluation about 27 s; adapter 125,849,896 B / 22 tensors; reload parity exact; the committed notebook blob `40e0a136` (commit `9bcb449`) executed 11/11 cells in 226.9 s (pre-flight harness; recorded in `docs/release-verification.md`). Peak CPU RAM was not measured; the float32 weights alone need about 2.4 GB, and training two layers adds their gradients and AdamW moments (about 378 MB).
- Tutorial execution (supported runtime): `tutorials/qwen3_embedding_colab.ipynb` blob `f3475b93` at `ec6dd95` ran top-to-bottom on a clean Kaggle Tesla T4 kernel (`kurtvalcorza/dimer-nb2-qwen3-embedding` v3, `torch 2.14.0+cu130`, `transformers 4.57.6`) on 2026-09-19: 11/11 ok (1 restart after install cell), 323.6 s, 26 files, 1209 MB fetched from the Hub; comparison {recall@1: {random_floor: 0.013, lexical: 0.3065, frozen: 0.6312, adapted: 0.7662}, recall@5: {random_floor: 0.0649, lexical: 0.5039, frozen: 0.8857, adapted: 0.9714}, recall@10: {random_floor: 0.1299, lexical: 0.6753, frozen: 0.9377, adapted: 0.9896}, mrr: {random_floor: 0.064, lexical: 0.4159, frozen: 0.741, adapted: 0.8498}, median_rank: {lexical: 5, frozen: 1, adapted: 1}, delta_vs_frozen: {recall@1: 0.1351, recall@5: 0.0857, recall@10: 0.0519, mrr: 0.1087}}; recorded in `docs/release-verification.md`.
- Tests: `pytest -q -o addopts= tests` — 40 passed offline without weights, plus 5 notebook-parity tests (2 further model-backed tests run when the snapshot is staged: the frozen retrieval against the lexical baseline on a dozen pairs, and a 1-epoch adaptation of the last layer with an artifact round trip); `ruff check src tests tools` clean.
- Not executed: the CUDA/bfloat16 path (inference or training), the `allow_download=True` Hub path, inputs near the 8,192-token ceiling against the real model, adaptation with more than four trainable layers or on a corpus other than Banking77.

## References

- Zhang, Y. et al. (2025). Qwen3 Embedding: Advancing Text Embedding and Reranking Through Foundation Models. https://arxiv.org/abs/2506.05176
- Upstream model card: https://huggingface.co/Qwen/Qwen3-Embedding-0.6B (pinned README, revision above)
- Upstream code: https://github.com/QwenLM/Qwen3-Embedding
- Sibling reranker: https://github.com/kurtvalcorza/qwen3-reranker-pipeline
- Casanueva, Temčinas, Gerz, Henderson, Vulić. Efficient Intent Detection with Dual Sentence Encoders. NLP4ConvAI 2020 (Banking77, CC BY 4.0). https://arxiv.org/abs/2003.04807 — https://github.com/PolyAI-LDN/task-specific-datasets
