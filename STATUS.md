# Release status

Current status: **Candidate — tutorial source complete, awaiting clean-runtime execution**. The pipeline package with its adaptation contract (`evaluate`, `lexical_baseline`, `adapt`, `save_artifact` / `from_artifact`; the pinned Banking77 pair corpus and BYOD loaders in `samples.py`; recall@k, MRR, the random floor and the lexical baseline in `metrics.py`), offline unit tests (40, no weights required, plus 5 notebook-parity tests and 2 model-backed tests that run when the snapshot is staged), a local CPU execution of the committed notebook blob in the Windows venv (frozen test recall@1 63.4 % → adapted 80.3 % over 77 intent documents against a lexical baseline of 30.6 %; recorded in `docs/release-verification.md`), `MODEL_CARD.md` (MODEL_CARD_SPEC 1.1), and the standalone `E2E` tutorial notebook `tutorials/qwen3_embedding_colab.ipynb` (generated by `tools/build_notebook.py`, DIMER Notebook Specification 2.0 §4) with its static validator, generator parity checks and CI workflow exist. Unit tests and static conformance checks are expected to run in GitHub Actions. The earlier `TASK-INFERENCE` notebook's Kaggle T4 run (blob `7715612eea32`, commit `cbeec85`) is retained as history and is not evidence for the `E2E` blob. The tutorial remains non-release-grade until clean-runtime execution evidence for the exact candidate commit is recorded according to `docs/release-verification.md`.
